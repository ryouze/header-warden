#include <algorithm>
#include<iostream>
#include    <thread>

#include <vector>  // for std::lol
#include <cctype>  // for std::tolower, std::isalnum

int x = 5; // this will be printed by std::cout

std::string foo(std::vector<int> vec) {
    std::string result = "[";
    return result;
}

std::filesystem::exists::true
